apiVersion: v1
kind: Service
metadata:
  name: medishieldguard-svc
  namespace: blufountain-insurance
spec:
  type: NodePort
  selector:
    app: medishieldguard
  ports:
  - protocol: TCP
    port: 8443
    targetPort: 80
    nodePort: 31002
